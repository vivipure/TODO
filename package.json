{
  "name": "todo",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . 'TODO' --overwrite --platform=win32 --arch=x64 --icon=./assets/logo/logo.ico --out=./out --asar --app-version=0.0.1 --version-string.ProductName=\"TODO\""
  },
  "author": "vivipure",
  "license": "MIT",
  "dependencies": {
    "electron": "^4.1.0"
  },
  "devDependencies": {
    "asar": "^1.0.0",
    "electron": "^4.1.0",
    "electron-packager": "^13.1.1"
  }
}
